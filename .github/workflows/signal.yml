name: Pro Signal Bot

on:
  schedule:
    - cron: '0 6 * * *'    # Toshkent 11:00 — London sessiyasi
    - cron: '0 11 * * *'   # Toshkent 16:00 — London + NY eng kuchli
    - cron: '0 15 * * *'   # Toshkent 20:00 — NY o'rtasi + Crypto
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  signal:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4.2.2

      - uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.11'

      - name: Kutubxonalar
        run: pip install requests pandas

      - name: Signal tahlili
        env:
          TELEGRAM_TOKEN:   ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TWELVE_DATA_KEY:  ${{ secrets.TWELVE_DATA_KEY }}
        run: python bot.py
